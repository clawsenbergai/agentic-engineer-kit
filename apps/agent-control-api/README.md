# agent-control-api

Publieke API laag voor task intake, approval decisions en run status query.

Suggested endpoints:
- `POST /tasks`
- `GET /tasks/:id`
- `POST /approvals/:id/decision`
- `GET /runs/:id`
