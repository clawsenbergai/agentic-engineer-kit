# control-plane service

Policy decisions, budget gates, risk classification, and run registry writes.

Inputs:
- task context
- tenant policy
- projected cost/value

Output:
- allow / require_approval / deny / route_human
