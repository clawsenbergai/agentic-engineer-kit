# WebMCP Track

WebMCP focuses on browser-facing model context and agent-ready web surfaces.

## Focus
- `navigator.modelContext` patterns
- Browser capability constraints
- Safe fallback when WebMCP is unavailable
